{
    "root": true,//look for everything up to the root directory.  "This IS a root, don't go look higher
    //eslint recommended rules
    "extends": [
      "eslint:recommended",
      "plugin:import/errors",
      "plugin:import/warnings"
    ],
    "parserOptions": {
      "ecmaVersion": 7,
      "sourceType": "module"
    },
    //expecting to work with...
    "env": {
      "browser": true,
      "node": true,
      "mocha": true
    },
    //rules to override
    "rules": {
        "no-console":1 //=1=warning... don't output to the console if if it iswarning
    }
  }
